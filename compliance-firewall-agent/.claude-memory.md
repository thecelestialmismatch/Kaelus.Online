# Claude Memory Log — Kaelus.ai
# Auto-populated by git post-commit hook + enriched each session
# READ THIS at the start of every session. Do NOT re-scan the project.

## GIT HISTORY
2026-03-14 20:49 | 2ae7928 UI styling updates and alignment fixes across application
2026-03-14 20:52 | 76980cc chore: update claude memory log with latest session entry
2026-03-14 20:52 | 0f6a4ca chore: sync claude memory log after hook update
2026-03-14 20:52 | f03f849 chore: final memory log sync
2026-03-15 13:48 | 330eca6 feat: frontend polish, auth improvements, and project setup
2026-03-15 XX:XX | session-002 code audit + skill system created

## PROJECT IDENTITY
Product: AI compliance firewall for CMMC Level 2 defense contractors
Market: 87,000+ DIB companies need CMMC by 2026. $50-500/mo budget.
Pricing: Free | Pro $69/mo | Enterprise $249/mo | Agency $599/mo
Current MRR: $0 — not launched yet
Active branch: feat/branding-shieldready-polish

## REAL BUILD STATUS (verified 2026-03-15)
DONE + WIRED: Landing page, Login, Signup, Auth middleware, Dashboard (main), Pricing, Stripe checkout/webhook/portal API routes, AI chat API, Agent execute API, Agent orchestrator, Gateway (stream+WS+scanner), All 110 NIST controls, SPRS scoring, PDF report API (JSON only), 3 Supabase migrations, PostHog installed, Privacy/Terms pages, sitemap, robots
DONE UI — MOCK DATA ONLY: Live scanner, Quarantine panel, Realtime feed, Agent workspace, Agent builder, AI chat component, Knowledge base, Team view, Tasks board
MISSING: All 15 env vars empty, PDF actual file generation (has JSON, not PDF), Real-time WebSocket to Supabase, Subscription feature gating

## ENV VAR STATUS (as of 2026-03-15)
.env.local has ONLY: VERCEL_OIDC_TOKEN (Vercel already connected, hobby tier)
ALL OTHERS ARE EMPTY: Supabase, Stripe, OpenRouter, Resend, PostHog, Encryption key

## DESIGN SYSTEM
bg-[#07070b] | brand-* tokens (indigo, NOT blue-*) | emerald accent
Typography: Inter body, Outfit display, font-mono for code
Icons: lucide-react only | Animation: framer-motion v12
Logo: ALWAYS <Logo /> and <TextLogo /> — never inline Shield/Zap icons
RULE: Replace ALL blue-* Tailwind classes with brand-* equivalents

## ARCHITECTURE DECISIONS
Auth: Supabase SSR client (server) + browser client (client components)
AI: OpenRouter free models (Gemini Flash, DeepSeek, Llama 3.3)
Stripe: price IDs in env only, never frontend
RLS: Required on every Supabase table
Build: Run `npm run build` before declaring anything done
Component size: max 500 lines — split if larger

## KEY FILE MAP
app/login + signup: Supabase Auth wired, Google/GitHub/Microsoft OAuth ready
app/pricing/page.tsx: 880 lines, Stripe handleCheckout wired (needs price IDs)
app/command-center/page.tsx: 805 lines, main dashboard with mock data
app/api/stripe/checkout|webhook|portal: Written, needs env vars
app/api/chat/route.ts: 238 lines, OpenRouter streaming
lib/shieldready/: All 110 NIST controls + SPRS scoring (4,500+ lines)
supabase/migrations/: 3 files ready to apply
.env.example: Has all var names — just fill in values

## COMPETITORS
Nightfall AI (enterprise $$$$), CalypsoAI (govt contracts), Robust Intelligence (F500)
Our edge: SMB-friendly, affordable, CMMC-specific, self-hosted option

## OPEN BLOCKERS
1. .env.local empty — everything depends on this first
2. Dashboard still has blue-* classes (last agent hit token limit)
3. Light mode CSS variables not styled (globals.css)
4. PDF reports API returns JSON — need jsPDF to render actual PDF file
5. Subscription gating not implemented in frontend

## CURRENT ACTIVE TASK
Scan ALL files for blue-* Tailwind classes → replace with brand-*
Files most likely affected: dashboard components, command-center sub-pages
